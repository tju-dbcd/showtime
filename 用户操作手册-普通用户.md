# SHOWTIME 用户操作手册（普通用户版）> 本手册面向普通购票用户，覆盖从注册登录到选座购票、支付、退票改签、电子票核销的完整流程。---## 目录1. [账号相关](#1-账号相关)2. [浏览与搜索演出](#2-浏览与搜索演出)3. [查看场次与选座](#3-查看场次与选座)4. [下单与支付](#4-下单与支付)5. [我的订单](#5-我的订单)6. [退票与改签](#6-退票与改签)7. [电子票](#7-电子票)8. [个人中心](#8-个人中心)9. [实时通知](#9-实时通知)10. [常见问题](#10-常见问题)---## 1. 账号相关### 1.1 注册1. 打开首页，点击右上角「注册」进入注册页。2. 填写用户名、手机号、密码等必填信息。3. 点击「注册」，成功后自动跳转登录页。> 注册接口有频率限制（同一 IP 3 次/分钟），频繁操作会收到 `429 RATE_LIMIT_EXCEEDED` 提示。### 1.2 登录1. 在登录页输入用户名/密码。2. 登录成功后返回 **Access Token（15 分钟有效）** 与 **Refresh Token（7 天有效）**，前端自动存储并在失效前静默刷新。3. 每个账号仅允许**一个活跃会话**：在新设备登录会使旧会话失效。### 1.3 退出登录- 在「个人中心 → 退出登录」确认后退出当前会话。---## 2. 浏览与搜索演出- **首页**：展示演出列表（海报、名称、场次概览）。- **搜索页**：按关键词检索演出。- 点击任意演出卡片进入「场次详情页」，查看该演出的各场次、票价区间等信息。---## 3. 查看场次与选座### 3.1 进入选座在场次详情页点击「选座购票」，进入选座页。### 3.2 座位图- 座位图按**分区（Section）**划分，每个分区有独立名称与颜色。- 座位状态用颜色区分（图例见页面底部）：| 颜色 | 状态 | 说明 ||------|------|------|| 绿色 | 可选 | 可以点选 || 蓝色 | 已选 | 你当前已选中的座位 || 橙色 | 锁定中 | 已被其他用户临时锁定 || 灰色 | 已售/不可用 | 无法选择 |- 点击座位即可选中/取消；选中后底部实时显示「已选 N 个座位」与「合计金额」。- 若存在多个场次，可在顶部「选择场次」切换。### 3.3 确认选座（锁座）点击「确认选座」后，系统会：1. **锁定座位**（默认锁期 600 秒），生成每个座位的 `LockToken`。2. **创建订单**，订单进入「待支付」状态，并带过期倒计时。> 锁座成功但未及时下单/支付，锁期到期后座位会自动释放，需重新选座。---## 4. 下单与支付### 4.1 下单选座确认后系统自动创建订单（`PENDING_PAY` 待支付），并跳转到「我的订单」列表。### 4.2 支付1. 在「我的订单」列表中找到待支付订单，点击「去支付」（或底部「立即付款」）。2. 弹出支付窗口，选择**微信支付**或**支付宝**（当前为 Mock 支付）。3. 支付成功后订单状态自动流转为「已出票」，并生成电子票。### 4.3 取消订单- 待支付订单可点击「取消」主动取消（取消后座位释放）。- 超过过期时间未支付，订单会被后台自动取消。---## 5. 我的订单「我的订单」页以分页表格展示全部订单，包含字段：- 订单号、票数、金额、状态、下单时间、过期时间。订单状态与对应操作：| 状态 | 显示 | 可执行操作 ||------|------|-----------|| `PENDING_PAY` | 待支付 | 去支付 / 取消 || `PAID` / `ISSUED` | 已支付 / 已出票 | 查看详情（可退票/改签） || `PART_REFUND` | 部分退款 | 查看详情 || `REFUNDED` | 已退款 | — || `CANCELLED` | 已取消 | — |点击「查看详情」进入订单详情页，可查看票品明细、电子票、以及发起退票/改签。---## 6. 退票与改签> 退票/改签入口仅在订单状态为「已支付/已出票」时可用。### 6.1 退票1. 订单详情页点击「申请退票」。2. 系统弹出退票报价：**可退金额、退票费率、服务费**（由退票策略按距开演时间阶梯计算）。3. 填写退票原因，点击「提交退票申请」。4. 提交后进入**管理员审核**；审核通过后系统异步执行退款，款项原路退回。5. 退款完成后订单状态变为「已退款」（全部）或「部分退款」（部分明细）。> 退票状态可通过实时通知获知：`退款处理中 → 退款已完成/失败`。### 6.2 改签> 当前改签规则：**仅支持同演出换场次**（跨演出改签暂不支持），且需 **1:1 换票**（换几张票选几个目标座位）。1. 订单详情页点击「申请改签」。2. 选择目标场次（仅显示仍在售的其他场次）。3. 跳转选座页选择目标座位（数量必须与原票一致）。4. 返回订单详情，点击「获取改签报价」，查看原票价、新票价、差价、改签费。5. 提交改签申请，等待管理员审核。6. 审核通过后，若需**补差价**，在「改签申请」面板点击「支付差价」完成改签。---## 7. 电子票- 订单出票后，在「订单详情 → 电子票」区查看每张票的**票号、状态、二维码**。- 电子票状态：`UNUSED`（待核销）/ `USED`（已核销）/ `REFUNDED`（已退）/ `EXCHANGED`（已改签）。- 入场时向检票人员出示二维码，由检票设备核销。---## 8. 个人中心「个人中心」左侧菜单包含：| 菜单 | 功能 ||------|------|| 个人资料 | 查看/编辑昵称、手机号、邮箱；**上传头像**（经 OSS 存储） || 账号安全 | 修改密码、绑定手机/邮箱 || 实名认证 | 查看实名状态、提交实名信息（部分演出购票需先实名） || 我的订单 | 订单概览，可跳转「我的订单」查看全部 || 收货地址 | 新增/编辑/删除收货地址 || 退出登录 | 退出当前会话 |---## 9. 实时通知系统通过 **SignalR** 向前端推送实时通知，无需手动刷新：- **下单成功**：创建订单后弹出「订单创建成功」通知。- **退款状态变化**：退票审核/退款进度更新时弹出通知，并自动刷新订单列表/详情。---## 10. 常见问题| 问题 | 说明 ||------|------|| 选座时提示「座位已被锁定」 | 该座位正被其他用户锁定，请选择其他座位或稍后再试 || 下单时提示「锁座已失效」 | 锁座已过期（600 秒）或被释放，请重新选座下单 || 支付时提示「订单已过期」 | 订单超过支付期限，已被取消，需重新下单 || 退票能退多少钱 | 由退票策略按「距开演时间」阶梯计算费率与服务费，申请时页面会展示报价 || 改签为什么只能同演出换场次 | 当前后端契约为同演出换场次，跨演出改签暂未开放 || 收不到通知 | 确认已登录且网络正常；通知为辅助提示，订单状态以列表/详情查询为准 |