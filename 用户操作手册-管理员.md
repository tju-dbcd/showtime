# SHOWTIME 用户操作手册（管理员版）

> 本手册面向运营/管理人员，覆盖管理后台（`/admin`）的演出、场次、座位、订单、退改签审核与策略配置等操作。
> 所有管理端接口均要求 **`Admin` 角色**（`[Authorize(Roles = "Admin")]`），登录账号需具备管理员权限。

---

## 目录

1. [后台概览](#1-后台概览)
2. [数据看板](#2-数据看板)
3. [演出管理](#3-演出管理)
4. [场次管理](#4-场次管理)
5. [座位图管理](#5-座位图管理)
6. [订单管理](#6-订单管理)
7. [退票审核](#7-退票审核)
8. [改签审核](#8-改签审核)
9. [退票 / 改签策略配置](#9-退票--改签策略配置)
10. [营销配置](#10-营销配置)
11. [演出发布](#11-演出发布)

---

## 1. 后台概览

访问 `/admin` 进入管理后台，左侧导航包含 7 个模块：

| 菜单 | 路由 | 说明 |
|------|------|------|
| 数据看板 | `/admin/dashboard` | 核心指标概览 |
| 演出管理 | `/admin/performance` | 演出列表与状态管理 |
| 场次管理 | `/admin/session` | 场次 CRUD、调价、状态 |
| 订单管理 | `/admin/order` | 订单列表、详情、取消、出票 |
| 演出发布 | `/admin/publish` | 新建演出并上传海报 |
| 座位图管理 | `/admin/seat-map` | 座位图可视化编辑 |
| 营销配置 | `/admin/marketing` | 营销内容管理 |

> 后端管理员接口统一在 `/api/admin/*` 下；退票/改签审核、策略配置等目前通过后端接口（Scalar 文档）操作，前端页面按需接入。

---

## 2. 数据看板

展示平台运营核心指标（如订单量、销售额、在售场次等），用于日常运营监控。

---

## 3. 演出管理

对应接口：`GET/POST /api/admin/shows`、`GET/PUT/DELETE /api/admin/shows/{showId}`。

- **新建演出**：在「演出发布」页填写演出信息、上传海报（走统一文件上传，存储于 OSS）。
- **查看列表**：分页展示全部演出。
- **编辑 / 删除**：修改演出信息或删除演出。
- **状态**：`SHOW.STATUS` 为 `DRAFT`（草稿）/ `PUBLISHED`（已发布）/ `UNPUBLISHED`（已下架）；`AUDIT_STATUS` 为 `PENDING / APPROVED / REJECTED`（审核状态）。

---

## 4. 场次管理

对应接口（均在 `/api/admin`）：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/shows/{showId}/sessions` | 某演出的场次列表 |
| POST | `/api/admin/shows/{showId}/sessions` | 为演出新建场次 |
| POST | `/api/admin/sessions/{sessionId}/pricing-strategies` | 配置票价策略 |
| POST | `/api/admin/sessions/{sessionId}/dynamic-pricing-rules` | 配置动态调价规则 |
| PUT | `/api/admin/sessions/{sessionId}/status` | 修改场次状态 |

**场次状态**（`SESSION_STATUS`）：`UPCOMING`（未开售）→ `PRESALE`（预售）→ `ONSALE`（在售）→ `SOLD_OUT`（售罄）→ `ENDED`（已结束）。

**操作要点**：
- 新建场次需绑定座位图（`SEAT_MAP_ID`）、起止时间、开售时间等。
- 票价策略按分区（`SeatSection`）配置，支持早鸟价/预售/标准/VIP 等类型。
- 动态调价规则可让票价随距开演时间自动变化（阶梯定价）。

---

## 5. 座位图管理

对应接口（均在 `/api/admin`）：

| 资源 | 接口 |
|------|------|
| 座位图 Seat Map | `GET/POST /api/admin/seat-maps`、`GET/PUT/DELETE /api/admin/seat-maps/{id}` |
| 分区 Section | `GET/POST /api/admin/seat-maps/{id}/sections`、`GET/PUT/DELETE /api/admin/seat-sections/{id}` |
| 座位 Seat | `GET/POST/PATCH /api/admin/seat-sections/{id}/seats`、`GET/PUT/DELETE /api/admin/seats/{id}` |
| 选座规则 Rule | `GET/POST /api/admin/seat-rules`、`GET/PUT/DELETE /api/admin/seat-rules/{id}`，及其 `scopes` |

**层级关系**：`场馆 → 座位图(Seat Map) → 分区(Seat Section) → 座位(Seat)`。

- 「座位图管理」页提供可视化编辑器，可拖拽分区、批量建座、保存模板。
- 座位字段含 `ROW_CODE / SEAT_NO / ROW_INDEX / COL_INDEX / X/Y_COORD` 与状态 `ENABLED / DISABLED / MAINTENANCE`。
- 选座规则支持连续座位、不留单座、限购等（`RULE_TYPE`），通过 `SEAT_RULE_SCOPE` 指定适用范围。

---

## 6. 订单管理

对应接口：`GET /api/admin/orders`、`GET /api/admin/orders/{orderId}`、`PATCH /api/admin/orders/{orderId}/cancel`、`POST /api/admin/orders/{orderId}/issue`。

- **订单列表**：支持按状态筛选、按订单号/用户名/昵称/手机号搜索、分页。
- **订单详情**：查看订单基本信息 + 票品明细（座位、定价策略、单价、明细状态）。
- **取消订单**：仅待支付（`PENDING_PAY`）订单可取消。
- **补出票**：`POST .../issue` 用于出票补偿场景。

订单状态（`ORDER_STATUS`）：`PENDING_PAY / PAID / ISSUED / PART_REFUND / REFUNDED / CANCELLED`。

---

## 7. 退票审核

对应接口：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/refunds` | 退票申请列表 |
| GET | `/api/admin/refunds/{refundId}` | 退票申请详情 |
| POST | `/api/admin/refunds/{refundId}/approve` | 审核通过 |
| POST | `/api/admin/refunds/{refundId}/reject` | 审核驳回 |

**流程**：用户申请退票 → 管理员审核 → 通过后系统异步执行退款（MQ 消费 `RefundApprovedEvent`）→ 退款完成，订单流转为 `REFUNDED` / `PART_REFUND`，并推送实时通知。

> 审核通过后不可逆，请先核对退款金额、退票费率与服务费。

---

## 8. 改签审核

对应接口：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/exchanges` | 改签申请列表 |
| GET | `/api/admin/exchanges/{exchangeId}` | 改签申请详情 |
| POST | `/api/admin/exchanges/{exchangeId}/approve` | 审核通过 |
| POST | `/api/admin/exchanges/{exchangeId}/reject` | 审核驳回 |

**流程**：用户提交改签（同演出换场次，1:1 换票）→ 管理员审核 → 通过后生成改签子订单，若需**补差价**由用户支付 → 改签完成。

---

## 9. 退票 / 改签策略配置

对应接口：

| 资源 | 接口 |
|------|------|
| 退票策略 | `GET/POST /api/admin/refund-policies`、`PUT /api/admin/refund-policies/{id}`、`PATCH /api/admin/refund-policies/{id}/status` |
| 改签策略 | `GET/POST /api/admin/exchange-policies`、`PUT /api/admin/exchange-policies/{id}`、`PATCH /api/admin/exchange-policies/{id}/status` |

**退票策略字段**：`REFUND_DEADLINE_HOUR`（距开演截止小时）、`REFUND_RATE`（退票费率）、`SERVICE_FEE`（服务费）、`PRIORITY`（优先级）、`SHOW_ID`（可针对单演出覆盖默认策略）。

**改签策略字段**：`EXCHANGE_DEADLINE_HOUR`、`EXCHANGE_FEE`（改签费）、`ALLOW_CROSS_SESSION`（是否允许跨场次）。

> 策略按 `PRIORITY` 匹配，`SHOW_ID` 为空表示全局默认；通过 `status` 上下线策略。

---

## 10. 营销配置

对应接口：`POST/PUT/DELETE/GET /api/admin/marketing-contents`（含 `/{contentId}`）。

- 管理演出关联的营销内容（推荐位、广告 Banner、通知等），`CONTENT_TYPE`：`NOTICE / AD / PROMOTION`。
- 图片素材通过统一文件上传接口（`POST /api/files/upload`）存储于 OSS。

---

## 11. 演出发布

- 在「演出发布」页填写演出基本信息（名称、分类、标签、场馆等），上传海报图片。
- 图片上传走后端代理（`POST /api/files/upload`，需认证），AccessKey 只存后端，前端不接触 OSS 凭据。
- 发布后演出状态变为 `PUBLISHED`，用户端即可浏览与购票。

---

## 附：管理员接口速查

| 模块 | 前缀 |
|------|------|
| 演出/场次 | `/api/admin/shows*`、`/api/admin/sessions*` |
| 座位 | `/api/admin/seat-maps*`、`/api/admin/seat-sections*`、`/api/admin/seats*`、`/api/admin/seat-rules*` |
| 订单 | `/api/admin/orders*` |
| 退票审核 | `/api/admin/refunds*` |
| 改签审核 | `/api/admin/exchanges*` |
| 退票策略 | `/api/admin/refund-policies*` |
| 改签策略 | `/api/admin/exchange-policies*` |
| 票务核销 | `/api/admin/tickets/redeem` |
| 营销内容 | `/api/admin/marketing-contents*` |

> 完整字段与示例以开发环境 Scalar 文档（`http://localhost:5146/scalar/v1`）为准。
