# 项目开发计划
## 项目时间线
## 一、项目时间线总览
> 完成的任务，通过~~标出

| 周次 | 前端 | 后端 | 测试/部署 | 文档/设计 |
|:----:|:-----|:-----|:----------|:----------|
| 1 | ~~确定技术栈、数据库设计、git 仓库~~ | ~~同上~~ | ~~同上~~ | ~~同上~~ |
| 2 | ~~搭建 React 项目骨架，~~根据 API 文档生成 TS 类型定义；~~搭建路由与页面占位~~ | ~~搭建 ASP.NET Core 项目骨架，产出 OpenAPI 文档（Scalar）；完成核心实体建模与 EF Core 映射~~ | 准备虚拟数据集（演出、场次、座位、用户）；配置 CI 流水线骨架（build + lint） | 完善 Figma 设计稿（用户端 3 页 + 管理端 2 页）；编写数据库 ER 图 |
| 3 | 用户端：登录/注册页、演出列表页、场次详情页；管理端：布局框架 + 演出发布页 | 用户注册/登录（JWT + 角色）；演出场次 CRUD API；座位区域管理 API；支付接口模拟（Mock） | 编写核心购票流程自动化测试脚本（Playwright）：注册→登录→浏览→选座→下单 | Oracle 自动备份脚本（RMAN 或 expdp）；撰写 API 对接文档（含错误码表） |
| 4 | 选座页面（可视化座位图）；我的订单页；接入真实 API 跑通登录→选座→下单全链路 | 选座锁座（Redis 分布式锁防超卖）；订单创建/支付/状态流转；退票改签基础逻辑；操作日志记录（Serilog） | 前后端联调测试；Playwright 端到端测试覆盖全链路 | 更新 API 对接文档（随接口变化同步）；编写联调问题记录 |
| 5 | 管理端：订单管理页、用户分析看板；演出营销配置页；对接后端进阶 API | 订单智能拆单/合并；退票改签策略引擎（规则配置化）；场次动态调价（阶梯定价）；RabbitMQ 异步消息（订单状态通知） | 搭建 Loki+Grafana 日志监控看板；JMeter 编写核心接口压测脚本（登录、下单、选座） | 绘制时序图（下单全流程，PlantUML）；绘制状态机图（订单生命周期） |
| 6 | 根据压测反馈优化前端（虚拟列表、懒加载、图片压缩）；管理端：可视化座位图编辑器 | SQL 性能优化（索引、查询改写）；冻结 develop 分支，只修 P0 级崩溃 Bug；Code Review 全量后端代码 | 全量回归测试（手动 + 自动化）；JMeter 分布式压测并输出优化建议报告 | 绘制 ER 图终版；编写部署文档（环境依赖、配置项清单） |
| 7 | 修复 UI 兼容性问题（浏览器、分辨率）；打包优化（Code Splitting、Tree Shaking）；交互动效打磨 | 修复剩余的 P1/P2 缺陷；电子票务生成（二维码防伪）；会话安全加固（Token 刷新、异地登录检测） | 安全测试（OWASP Top 10 基础项：XSS、CSRF、SQL 注入、越权）；性能回归验证 | 撰写用户操作手册；准备答辩 PPT 初版 |
| 8 | 最终验收 Demo 准备；上线前检查清单逐项确认 | 数据库迁移脚本最终核对；环境配置最终确认（生产/测试分离） | 上线演练（模拟部署全流程）；监控告警规则配置 | 答辩 PPT 定稿 + 演练；项目总结文档；代码仓库归档整理 |

---

## 二、里程碑节点

| 里程碑 | 时间 | 验收标准 |
|:-------|:-----|:---------|
| M1 - 骨架搭建完成 | 第 2 周末 | 前后端项目可本地运行；Scalar 可查看全部 API 定义；Figma 设计稿评审通过 |
| M2 - 核心链路跑通 | 第 4 周末 | 用户可完成注册→登录→浏览演出→选座→下单→支付（Mock）→查看订单的全流程；端到端自动化测试通过 |
| M3 - 功能冻结 | 第 6 周末 | 所有规划功能开发完毕；压测关键接口 QPS 达标；develop 分支冻结，不再合入新 Feature |
| M4 - 上线就绪 | 第 8 周末 | 全量回归测试通过；安全测试无高危漏洞；答辩材料完成；代码仓库归档 |

---

## 三、各角色每周详细任务

### 前端

| 周次 | 核心目标 | 具体任务 | 交付物 |
|:----:|:---------|:---------|:-------|
| 2 | 项目骨架 | ① 使用 Vite 初始化 React + TS 项目 ② 配置 Ant Design、React Router、Axios ③ 根据 OpenAPI 文档生成 TS 类型（openapi-typescript） ④ 搭建路由骨架与页面占位（空壳页） | 可运行的前端项目；路由配置完成 |
| 3 | 用户端核心页面 | ① 登录/注册页（表单校验、Token 存储） ② 演出列表页（分页、筛选、搜索） ③ 场次详情页（场次信息、票价展示） ④ 管理端布局框架（侧边栏 + 顶栏 + 内容区） ⑤ 演出发布页（表单 + 图片上传） | 用户端 3 页可交互；管理端布局可用 |
| 4 | 选座 + 全链路 | ① 可视化座位图组件（SVG/Canvas，支持分区、状态着色） ② 我的订单列表页（分状态 Tab） ③ 接入后端真实 API，替换 Mock 数据 ④ 全链路联调：登录→选座→下单→支付→查看订单 | 选座页面可用；全链路跑通 |
| 5 | 管理端 + 看板 | ① 管理端订单管理页（列表、筛选、状态操作） ② 用户分析看板（图表：ECharts/Recharts） ③ 演出营销配置页（推荐位、广告 Banner） ④ 对接 RabbitMQ 实时订单通知（SignalR/WebSocket） | 管理端功能完备；看板上线 |
| 6 | 优化 + 编辑器 | ① 虚拟列表优化（react-window）处理大量演出/订单 ② 图片懒加载 + WebP 格式 ③ 可视化座位图编辑器（拖拽分区、保存模板） ④ 配合后端修复联调 Bug | 编辑器可用；性能优化完成 |
| 7 | 打磨 + 打包 | ① 浏览器兼容性修复（Chrome/Edge/Firefox） ② 响应式适配（管理端最低 1280px） ③ 打包优化：Code Splitting、Tree Shaking、Gzip ④ 交互动效细节打磨（加载态、空态、错误态） | 兼容性达标；打包体积优化 |
| 8 | 收尾 | ① Demo 环境部署与验证 ② 上线前检查清单逐项确认 ③ 配合答辩准备现场演示流程 | Demo 可演示；上线就绪 |

### 后端

| 周次 | 核心目标 | 具体任务 | 交付物 |
|:----:|:---------|:---------|:-------|
| 2 | 项目骨架 | ① 创建 ASP.NET Core 8 Web API 项目 ② 配置 EF Core + Oracle（连接串、DbContext） ③ 完成核心实体建模（User、Show、Session、Seat、Order 等） ④ 配置 OpenAPI 文档（Scalar）自动生成 ⑤ 统一返回格式 + 全局异常处理中间件 | 可运行的后端项目；Scalar 可访问 |
| 3 | 核心 CRUD | ① 用户注册/登录 API（JWT 认证 + 角色授权） ② 演出/场次 CRUD API（含分页、筛选） ③ 座位区域管理 API ④ 支付接口 Mock（返回固定成功/失败，预留真实接口） | 核心 CRUD 全部可用 |
| 4 | 交易核心 | ① 选座锁座 → 下单 → 支付完整事务流程 ② Redis 分布式锁（防超卖，锁粒度：座位×场次） ③ 订单状态机（待支付→已支付→已出票→已核销 / 已取消→已退款） ④ 退票改签基础逻辑 ⑤ Serilog 操作日志（记录关键操作：下单、退票、登录） | 交易闭环完成；日志可用 |
| 5 | 进阶功能 | ① 订单智能拆单/合并（同场次多座位自动合并） ② 退票改签策略引擎（按距离开演时间阶梯扣费，规则可配置） ③ 场次动态调价（早鸟价/原价/临场价） ④ RabbitMQ 异步消息（下单成功→发通知；退款→更新状态） | 进阶功能全部上线 |
| 6 | 优化 + 冻结 | ① SQL 性能优化（索引分析、慢查询改写、N+1 问题修复） ② 冻结 develop 分支，停止合入新 Feature ③ 全量代码 Review（交叉审查，每人至少审查 2 个模块） ④ 只修复 P0 级崩溃/安全 Bug | 分支冻结；Review 完成 |
| 7 | 修 Bug + 安全 | ① 修复 P1/P2 级别缺陷 ② 电子票务生成（二维码内容加密 + 签名防伪） ③ 会话安全加固（Refresh Token、异地登录检测与踢出） ④ API 限流（Rate Limiting 中间件） | 电子票可用；安全加固完成 |
| 8 | 上线准备 | ① 数据库迁移脚本最终核对（baseline + migrations） ② 环境配置分离（appsettings.Development.json / Production.json） ③ 上线演练（模拟部署、回滚流程） ④ 最终版本打 Tag | 部署脚本就绪；Tag 已打 |

### 测试/部署

| 周次 | 核心目标 | 具体任务 | 交付物 |
|:----:|:---------|:---------|:-------|
| 2 | 基础设施 | ① 准备虚拟数据集（至少 10 场演出、每场 3-5 场次、每场次 200+ 座位） ② GitHub Actions CI 流水线骨架（前端 build + lint，后端 build + test） | 虚拟数据就绪；CI 可运行 |
| 3 | 自动化测试 | ① Playwright 环境搭建 ② 编写核心购票流程脚本（注册→登录→浏览→选座→下单） ③ Oracle 自动备份脚本（RMAN 全备 + 归档日志） | 自动化测试脚本；备份脚本 |
| 4 | 联调测试 | ① 前后端联调测试（逐接口核对） ② Playwright 端到端脚本扩展（含异常路径：超时未支付、重复下单） | 联调测试报告；E2E 脚本 |
| 5 | 监控 + 压测 | ① 搭建 Loki + Grafana（收集 Serilog 日志） ② 配置可视化监控大屏（QPS、错误率、响应时间） ③ JMeter 编写核心接口压测脚本 | 监控看板上线；压测脚本 |
| 6 | 压测执行 | ① JMeter 分布式压测执行（模拟 500/1000/2000 并发） ② 输出性能优化建议报告（瓶颈接口、慢 SQL、资源占用） | 压测报告 |
| 7 | 安全测试 | ① OWASP Top 10 基础测试（XSS、CSRF、SQL 注入、越权、敏感信息泄露） ② 性能回归验证（对比压测报告，确认优化效果） | 安全测试报告；性能回归报告 |
| 8 | 上线演练 | ① 模拟生产部署全流程（数据库迁移 → 后端部署 → 前端部署 → 冒烟测试） ② 配置监控告警规则（接口不可用、错误率飙升、CPU/内存阈值） ③ 准备回滚方案 | 部署手册；告警已配置 |

### 文档/设计

| 周次 | 核心目标 | 具体任务 | 交付物 |
|:----:|:---------|:---------|:-------|
| 2 | 设计 + 基础文档 | ① 完善 Figma 设计稿：用户端（演出列表、场次详情、选座、订单）+ 管理端（演出发布、订单管理） ② 数据库 ER 图（基于 baseline 建表脚本） ③ 编写 .editorconfig 和代码规范约定 | Figma 设计稿；ER 图 |
| 3 | API 文档 | ① 汇总后端 Scalar/OpenAPI 输出，编写 API 对接文档（请求/响应示例 + 错误码表） ② 维护接口变更日志 | API 对接文档 |
| 4 | 联调文档 | ① 随接口变化同步更新 API 对接文档 ② 记录联调中的典型问题及解决方案（FAQ） | 更新版 API 文档；FAQ |
| 5 | 技术图表 | ① 绘制时序图：下单全流程（用户→前端→后端→Redis→DB→MQ） ② 绘制状态机图：订单生命周期 ③ 绘制 ER 图终版 | PlantUML 图表集 |
| 6 | 部署 + 压测文档 | ① 编写部署文档（服务器环境、依赖清单、配置项说明） ② 汇总 JMeter 压测报告 | 部署文档；压测报告 |
| 7 | 用户手册 + PPT | ① 撰写用户操作手册（普通用户 + 管理员两个版本） ② 答辩 PPT 初版（含架构图、技术难点、解决方案） | 用户手册；PPT 初版 |
| 8 | 答辩 + 归档 | ① 答辩 PPT 定稿 + 内部演练 ② 项目总结文档（技术选型回顾、遗留问题、后续规划） ③ 代码仓库 README 更新，添加运行指南 ④ 归档所有设计稿、文档、脚本 | PPT 定稿；仓库归档 |

---