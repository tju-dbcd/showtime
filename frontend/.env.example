# 前端环境变量模板
# 复制为 .env.development / .env.production 并按需填写；真实值由 CI/部署流程注入，禁止提交进仓库。

# API 基础地址。
# 留空 = 开发时走 vite proxy（见 vite.config.ts），或生产同域部署（nginx 把静态文件与 /api 反代放在同一域名）。
# 非空 = 跨域直连后端，例如 VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
